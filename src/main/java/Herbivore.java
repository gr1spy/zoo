/**
 * Класс "Травоядные"
 */
public class Herbivore extends Animal {

    public Herbivore(Long nextAnimalId, Boolean isPredator, String kindOfAnimal, Boolean noise, Boolean hungry, Boolean sleep) {
        super(nextAnimalId, isPredator, kindOfAnimal, noise, hungry, sleep);
    }
}
