/**
 * Класс "Хищники"
 */
public class Predator extends Animal {

    public Predator(Long nextAnimalId, Boolean isPredator, String kindOfAnimal, Boolean noise, Boolean hungry, Boolean sleep) {
        super(nextAnimalId, isPredator, kindOfAnimal, noise, hungry, sleep);
    }
}
